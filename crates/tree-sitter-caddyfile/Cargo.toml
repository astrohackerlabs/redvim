[package]
name = "tree-sitter-caddyfile"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Pinned official Caddyfile grammar with corrected heredoc scanning"
build = "build.rs"

[dependencies]
tree-sitter-language = "0.1.7"

[build-dependencies]
cc = "1"

[dev-dependencies]
tree-sitter = "0.25"
